// Access Code Entry Page — Meedhanshu will build this
// Input: 16-char hex access code
// Validates via /api/validate-code, stores in localStorage

export default function Access() {
    return (
        <div className="page-stub">
            <h2>🔑 Enter Access Code</h2>
            <p>Enter the access code you received via email to unlock private content</p>
        </div>
    );
}
